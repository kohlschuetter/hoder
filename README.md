#  hoder — Enable HDR for external displays on macOS

My external display sometimes loses its "HDR-enabled" state after resume from sleep.

This commandline tool can be run to enable HDR on all external displays.
